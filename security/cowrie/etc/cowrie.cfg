[ssh]
listen_endpoints = tcp:2222:interface=0.0.0.0

[logging]
jsonlogs_enabled = true
jsonlogs_write_readable = false

[shell]
# Fab synthétique qui ressemble à l'infra réelle de la plateforme
filesystem = filesystem.pickle

[hostname]
hostname = bluerock-app

[backend]
# Pas de backend externe : logs JSON + SQLite locaux suffisent